# Unbind all existing key bindings
unbind -a

set -a terminal-features "tmux-256color:RGB"
set -ga terminal-overrides ",xterm-256color:Tc"

set -g prefix C-t
set -g base-index 1
set -g renumber-windows on
set -g mode-keys vi
set -g status-style "bg=#eddeb5"
set -g status-left "#S "
set -g status-right ""
set -g window-status-current-style "fg=black bold, bg=#d7d9ae"
set -g mouse on
setw -g aggressive-resize on

bind g run-shell "tmux new-window -c '#{pane_current_path}' -n 'lazygit' lazygit"
bind d run-shell "tmux new-window -c '#{pane_current_path}' -n 'lazydocker' lazydocker"

bind-key n new-window -c "#{pane_current_path}"
bind-key j previous-window
bind-key k next-window
bind-key r command-prompt "rename-window '%%'"
bind-key / copy-mode \; command-prompt -p "Search up:" "send -X search-backward '%%'"
bind-key ? copy-mode \; command-prompt -p "Search down:" "send -X search-forward '%%'"
bind-key p split-window -h
bind-key P split-window -v
bind-key H select-pane -L
bind-key J select-pane -D
bind-key K select-pane -U
bind-key L select-pane -R
bind-key x kill-window
bind-key X kill-pane
bind-key o choose-session